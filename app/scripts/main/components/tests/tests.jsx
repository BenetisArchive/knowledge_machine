var React = require('react');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                Create tests
                Available tests
                Assign tests
            </div>
        );
    }
});